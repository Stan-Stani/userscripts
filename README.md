## azureDevopsPRs-hotkeys.user.js

`Ctrl + Right Arrow`: Show only active (unresolved) comments

`Ctrl + Left Arrow`: Show everything

`Windows Key / Mac Command Key + ]`: Copy source branch name to clipboard
